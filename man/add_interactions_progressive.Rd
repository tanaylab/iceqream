% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactions.R
\name{add_interactions_progressive}
\alias{add_interactions_progressive}
\title{Progressive two-pass interaction selection}
\usage{
add_interactions_progressive(
  traj_model,
  thresholds = c(0.01, 5e-04),
  only_sig_motifs = c(TRUE, FALSE),
  only_sig_add_motifs = c(TRUE, TRUE),
  additional_features_builder = NULL,
  interaction_scale_factor = 1,
  min_signal_correlation = NULL,
  seed = 60427,
  ...
)
}
\arguments{
\item{traj_model}{A \code{TrajectoryModel}.}

\item{thresholds}{A numeric vector of \code{interaction_threshold} values,
one per pass. Default \code{c(0.01, 0.0005)} mirrors Akhiad's pattern.}

\item{only_sig_motifs}{A logical vector matched to \code{thresholds}.
Default \code{c(TRUE, FALSE)}.}

\item{only_sig_add_motifs}{A logical vector matched to \code{thresholds}.
Default \code{c(TRUE, TRUE)}.}

\item{additional_features_builder}{NULL or a function taking the
traj_model after pass 1 and returning a data frame to cbind onto
\verb{@additional_features}. Use \code{\link[=default_score_split_features]{default_score_split_features()}} as a
ready-made builder when \code{atac_scores} is available.}

\item{interaction_scale_factor, min_signal_correlation}{Forwarded to
each \code{\link[=add_interactions]{add_interactions()}} call.}

\item{seed}{Integer seed forwarded to \code{\link[=add_interactions]{add_interactions()}}.}

\item{...}{Additional arguments forwarded to \code{\link[=add_interactions]{add_interactions()}}
(e.g. \code{max_motif_n}, \code{max_add_n}, \code{max_n}, \code{logist_interactions}).}
}
\value{
The updated trajectory model with interactions and any
engineered additional features from the builder.
}
\description{
Implements the two-pass interaction-selection pattern used manually in
Akhiad's analysis workflow:
}
\details{
\enumerate{
\item First pass: tight \code{interaction_threshold[1]}, typically with
\code{only_sig_motifs = TRUE} — a strict selection to seed the model with
high-confidence interactions.
\item (Optional) Call \code{additional_features_builder(traj_model)} to produce
engineered additional features (e.g. \code{base_pred}, \code{end_pred} from
\code{\link[=default_score_split_features]{default_score_split_features()}}) and inject them. Relearn once so
the next pass sees an enriched anchor set.
\item Second pass: looser \code{interaction_threshold[2]}, typically with
\code{only_sig_motifs = FALSE} and \code{force = TRUE} — broader selection
that can pick interactions anchored on the newly-injected features.
}

Without a feature builder, the two passes are equivalent to a single
\code{\link[=add_interactions]{add_interactions()}} call at the final (loosest) threshold, so the
primary use case is with \code{default_score_split_features} or a custom
builder. For data without multi-bin \code{atac_scores}, prefer a single
\code{\link[=add_interactions]{add_interactions()}} call with \code{interaction_threshold = thresholds[1]}.
}
