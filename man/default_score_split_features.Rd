% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactions.R
\name{default_score_split_features}
\alias{default_score_split_features}
\title{Build base-score / end-score engineered additional features}
\usage{
default_score_split_features(traj_model, atac_scores, bin_start, bin_end)
}
\arguments{
\item{traj_model}{A \code{TrajectoryModel} already fit with a first pass
of interactions (so the relearns have something meaningful to fit).}

\item{atac_scores}{A data frame / matrix of per-peak ATAC scores with
one column per bin, rows aligned with \code{traj_model@peak_intervals}.}

\item{bin_start, bin_end}{Column index (integer) or name (character) of
the start and end bins in \code{atac_scores}.}
}
\value{
A data frame with columns \code{base_pred}, \code{end_pred},
\code{pred_diff_e_b}, row-aligned with \code{traj_model@peak_intervals}.
}
\description{
Used by \code{\link[=add_interactions_progressive]{add_interactions_progressive()}} between passes. Relearns the
trajectory model twice — once with each end of \code{atac_scores} as the
response — and returns a data frame with three engineered columns
suitable for \code{cbind()} onto \verb{@additional_features}:
}
\details{
\itemize{
\item \code{base_pred}: prediction under the bin_start score, scaled to \verb{[0, 10]}.
\item \code{end_pred}: prediction under the bin_end score, scaled to \verb{[0, 10]}.
\item \code{pred_diff_e_b}: \code{end_pred - base_pred} scaled to \verb{[0, 10]}.
}

These features mirror the manual pattern in Akhiad's analysis
workflow and are what the second pass of
\code{\link[=add_interactions_progressive]{add_interactions_progressive()}} anchors interactions against.

Requires \code{atac_scores} with at least two distinct columns. If the
inputs don't support it, the caller should fall back to a single
\code{\link[=add_interactions]{add_interactions()}} call.
}
